// Task-4 : Fill the password using DOM
// Basically after writing some code it must contain our password
// document.getElementById("pass");
// We have something called 'value', if we try to fill it something -
document.getElementById("pass").value = "Mohammad";
document.getElementById("pass").type = "text"; // to make password visible

// Step-1 : Get the element
// Step-2 : Change the value

// Have you ever seen password manager that automatically fill the id & password?
// Basically they just run the similar script where it is filling the data in the
// input part